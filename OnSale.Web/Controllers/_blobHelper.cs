using System;
using System.Threading.Tasks;

namespace OnSale.Web.Controllers
{
    internal class _blobHelper
    {
        internal static Task<Guid> UploadBlobAsync(object imageFile, string v)
        {
            throw new NotImplementedException();
        }
    }
}